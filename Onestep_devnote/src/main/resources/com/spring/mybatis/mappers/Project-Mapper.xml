<?xml version="1.0" encoding="UTF-8"?>

<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "https://mybatis.org/dtd/mybatis-3-mapper.dtd">
  
  
<mapper namespace="Project-Mapper">


	<select id="selectProjectList" resultType="project">
		select *
		from project
		order by projectid desc
	</select>
	
	<select id="selectProjectByProjectId" parameterType="int" resultType="project">
		select *
		from project
		where projectid=#{projectId}
	
	</select>
	
	
	
	<insert id="insertProject" parameterType="project">
		insert into
		project(projectid, projectname, projectdescription)
		values(#{projectId}, #{projectName}, #{projectDesc})
	</insert>
	
	<update id="updateProject" parameterType="project">
		update project
		set 
		projectname=#{projectname}, projectdescription=#{projectdesc}
		where projectid=#{projectid}
	</update>
	
	
	<delete id="deleteProject" parameterType="int">	
		delete
		from project
		where projectid=#{projectid}
	
	</delete>

</mapper>